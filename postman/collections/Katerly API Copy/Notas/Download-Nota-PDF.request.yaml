$kind: http-request
name: Download Nota PDF
method: GET
url: '{{base_url}}/api/notas/:id/pdf'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
pathVariables:
  - key: id
    value: '1'
