$kind: http-request
name: Delete Nota
method: DELETE
url: '{{base_url}}/api/notas/:id'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
pathVariables:
  - key: id
    value: '1'
