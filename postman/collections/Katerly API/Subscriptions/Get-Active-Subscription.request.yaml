$kind: http-request
name: Get Active Subscription
method: GET
url: '{{base_url}}/api/subscriptions/active'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
