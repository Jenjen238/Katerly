$kind: http-request
name: Get Ingredient by ID
method: GET
url: '{{base_url}}/api/ingredients/:id'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
pathVariables:
  - key: id
    value: '1'
